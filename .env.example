# RedisLens Development Environment Variables
# Copy to .env and modify as needed: cp .env.example .env

# ─── Redis Connection (development) ─────────────────────────────
REDIS_URL=redis://localhost:6379

# ─── Tauri Signing (required for release builds with auto-update) ─
# Generate with: cargo tauri signer generate -w ~/.tauri/redis-lens.key
TAURI_SIGNING_PRIVATE_KEY=
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=

# ─── macOS Notarization (optional, for distribution) ─────────────
# APPLE_ID=your@apple.id
# APPLE_PASSWORD=app-specific-password
# APPLE_TEAM_ID=XXXXXXXXXX

# ─── Windows Code Signing (optional, for distribution) ───────────
# WINDOWS_CERTIFICATE=base64-encoded-pfx
# WINDOWS_CERTIFICATE_PASSWORD=

# ─── CI/CD ──────────────────────────────────────────────────────
# GITHUB_TOKEN=  (set automatically in GitHub Actions)

# ─── Development Flags ──────────────────────────────────────────
# RUST_LOG=debug
# RUST_BACKTRACE=1
